# R 语言

## deepin 上安装

```bash
sudo apt install r-base
```

输入以上命令即可。

# R Studio

## deepin 上安装

在官网下载 Linux 版本的 R Studio 安装包，下载完成后，在下载目录打开终端，输入以下命令进行安装：

```bash
sudo dpkg -i rstudio-2025.05.1-513-amd64.deb # 具体版本号可能有所不同
```
